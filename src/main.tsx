import React from "react";
import ReactDOM from "react-dom/client";
import SwapiList from "./pages/swapiList/index";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SwapiList />
  </React.StrictMode>,
);
